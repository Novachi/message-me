<div class="ui inverted small menu">
  <div class="ui container">
    <a class="item" href="/chatrooms">
      Chatroom
    </a>
    <a class="item">
      Messages
    </a>
    <div class="right menu">
      <div class="ui dropdown item">
        Account <i class="dropdown icon"></i>
        <div class="menu">
          <% if logged_in?%>
            <%= link_to 'Log out', logout_path, method: :delete, class: 'item'%>
          <% else %>
            <%= link_to 'Sign in', login_path, class: 'item' %>
            <%= link_to 'Sign up', register_path, class: 'item'%>
          <% end %>
        </div>
      </div>
      <% unless logged_in? %>
        <div class="item">
          <%= link_to 'Sign in', login_path, class: 'ui green button' %>
        </div>
      <% end %>
    </div>
  </div>
</div>